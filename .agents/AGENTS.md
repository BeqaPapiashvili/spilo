# User Guidelines & Project Rules

- **Font Weight Rule**: NEVER use `font-weight` (e.g. `font-bold`, `font-semibold`, `font-extrabold`, `font-black`, `font-medium`) on text anywhere without explicit user request. Keep typography weights clean and default unless requested otherwise.
